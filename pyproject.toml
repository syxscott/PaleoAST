[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "paleoast"
version = "1.0.1"
description = "Paleontological Advanced Statistical Toolkit - Professional desktop application for paleontological data analysis"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "PaleoAST Development Team"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "pandas>=2.0.0",
    "matplotlib>=3.7.0",
    "PyQt6>=6.5.0",
]

[project.optional-dependencies]
full = [
    "networkx>=3.1",
    "psutil>=5.9.0",
    "scikit-learn>=1.3.0",
    "statsmodels>=0.14.0",
    "mplcursors>=0.5.1",
    "openpyxl>=3.1.0",
    "xlrd>=2.0.1",
]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "mypy>=1.4.0",
    "ruff>=0.4.0",
]
docs = [
    "sphinx>=7.0.0",
    "sphinx-rtd-theme>=1.3.0",
]

[project.scripts]
paleoast = "main:main"

[tool.setuptools.packages.find]
include = [
    "config*",
    "controllers*",
    "ecology*",
    "hpc*",
    "macroevolution*",
    "models*",
    "morpho3d*",
    "morphometrics*",
    "parsers*",
    "phylogenetics*",
    "reporting*",
    "state_machine*",
    "statistics*",
    "stratigraphy*",
    "utils*",
    "views*",
    "visualization*",
    "app_infrastructure*",
]
exclude = [
    "tests*",
    "tests_morpho3d_macroevolution*",
]

# =============================================================================
# Ruff - Code Linting & Formatting
# =============================================================================
[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "RUF",   # ruff-specific rules
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "E741",   # ambiguous variable name (common in scientific code: l, O, I)
    "N802",   # function name should be lowercase (scientific convention: PCA, NMDS)
    "N803",   # argument name should be lowercase
    "N806",   # variable in function should be lowercase
    "UP007",  # use X | Y for union types (keep Optional for readability)
    "B007",   # unused loop control variable
    "SIM108", # use ternary operator (reduces readability for scientific code)
    "RUF012", # mutable class variables without ClassVar
    "RUF001", # ambiguous unicode character in string (Chinese characters)
    "RUF002", # ambiguous unicode character in docstring (Chinese characters)
    "RUF003", # ambiguous unicode character in comment (Chinese characters)
]

[tool.ruff.lint.per-file-ignores]
"tests_morpho3d_macroevolution/**/*.py" = ["E", "W", "F841"]
"test_regression.py" = ["E", "W", "F"]

[tool.ruff.lint.isort]
known-first-party = [
    "config", "controllers", "ecology", "models", "views",
    "statistics", "morphometrics", "morpho3d", "stratigraphy",
    "visualization", "utils", "phylogenetics", "macroevolution",
    "parsers", "hpc", "reporting", "state_machine", "app_infrastructure",
]

# =============================================================================
# Mypy - Static Type Checking
# =============================================================================
[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true
ignore_missing_imports = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = [
    "PyQt6.*",
    "matplotlib.*",
    "mplcursors.*",
    "sklearn.*",
    "statsmodels.*",
    "networkx.*",
    "psutil.*",
    "openpyxl.*",
    "xlrd.*",
]
ignore_missing_imports = true

# =============================================================================
# Pytest - Testing Configuration
# =============================================================================
[tool.pytest.ini_options]
testpaths = ["tests_morpho3d_macroevolution", "."]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
]

# =============================================================================
# Coverage
# =============================================================================
[tool.coverage.run]
source = [
    "config",
    "controllers",
    "ecology",
    "models",
    "statistics",
    "morphometrics",
    "morpho3d",
    "stratigraphy",
    "utils",
    "views",
    "visualization",
]
omit = [
    "tests*",
    "tests_morpho3d_macroevolution*",
    "app_infrastructure*",
    "test_regression.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
    "if TYPE_CHECKING:",
    "pass",
]
